<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doooly.dao.activity.ActActivityCodeRecordDao">

    <!-- 批量插入 -->
    <insert id="insertBatch">
        insert into activity_dev.act_activity_code_record
        (
        activity_id,
        act_code,
        state,
        create_date
        )
        values
        <foreach collection="keys" item="key" separator=",">
            (
            #{activityId},
            #{key},
            #{state},
            #{createDate}
            )
        </foreach>
    </insert>

    <select id="findByActivityIdAndUserId" resultType="com.doooly.entity.activity.ActActivityCodeRecord">
        SELECT
            id AS id,
            user_id AS userId,
            act_code AS actCode
        FROM
            act_activity_code_record
        WHERE
            activity_id = #{activityId}
            AND user_id = #{userId}
    </select>
</mapper>