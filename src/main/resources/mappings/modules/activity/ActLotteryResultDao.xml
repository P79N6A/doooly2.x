<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doooly.dao.activity.ActLotteryResultDao">

    <select id="getListByActivityIdAndLimit">
        SELECT
           id ,
           phone_no AS 'phoneNo',
           wechat_nick AS 'wechatNick',
           lottery_name As 'lotteryName'
        FROM act_lottery_result WHERE activity_id=#{activityId} LIMIT #{rowNum}
    </select>
</mapper>